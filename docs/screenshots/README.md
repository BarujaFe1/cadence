# Screenshots

Captured from the local quality-pass build (`pnpm dev`) on 2026-07-13.

| File | View | Notes |
| --- | --- | --- |
| `today.png` | Hoje | Idle routine, dark mode |
| `history.png` | Histórico | After loading demo seed |
| `settings.png` | Ajustes | Duration sliders |

## How to recapture

```bash
pnpm dev
# Open http://localhost:3000
# Ajustes → Carregar demo no histórico (optional)
# Capture viewport screenshots; no PII (local-only synthetic/demo data)
```

Avoid including the Next.js dev “Issues” badge in marketing crops when possible.
