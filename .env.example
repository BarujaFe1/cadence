# Cadence does not require environment variables for local use or Vercel deploy.
# Persistence is local-first (browser localStorage).

# Optional: only if you use Vercel CLI locally and it writes OIDC tokens.
# VERCEL_OIDC_TOKEN=
